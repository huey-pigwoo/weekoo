'use client';

import { Typography, Box } from '@mui/material';

export default function GrammarContent() {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h4">
        語法單元
      </Typography>
    </Box>
  );
} 